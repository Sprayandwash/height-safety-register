# Step 9B controlled-run history

Each file in this folder is a small, secret-free record of one controlled Staging Admin test run. Files are named by date and GitHub Actions run ID.

Do not overwrite a previous run record. Add a new file for each run and add a matching short row to `../../RUN-LEDGER.md`.

